<h1>編集画面</h1>
<%= form_with model: @author, url: author_path(@author), method: :patch do |form| %>
  <%= render 'form', form: form  %>

  <div>
    <%= form.submit "更新" %>
  </div>
<% end %>
<%= link_to "戻る", author_path(@author) %>
<%= link_to "一覧", authors_path %>
