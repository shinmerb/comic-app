<h1>編集画面</h1>
<%= form_with model: @publisher, url: publisher_path(@publisher), method: :patch do |form| %>
  <%= render 'form', form: form  %>

  <div>
    <%= form.submit "更新" %>
  </div>
<% end %>
<%= link_to "戻る", publisher_path(@publisher) %>
<%= link_to "一覧", publishers_path %>
